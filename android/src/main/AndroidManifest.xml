<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!--
    RECORD_AUDIO is auto-merged into the consuming app via manifest merger.
    MODIFY_AUDIO_SETTINGS is optional — add it in your app if needed.
  -->
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>
